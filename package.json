{
  "name": "qlik-sense-object-id-grabber",
  "version": "1.1.0",
  "private": true,
  "description": "Qlik Sense Object Grabber (self-contained bookmarklet generator)",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
